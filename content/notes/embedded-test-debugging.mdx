---
title: 嵌入式面试测试题 �?调试与故障案�?
slug: embedded-test-debugging
date: 2026-07-13
tags: ["HardFault", "调试", "栈溢出", "优先级翻转", "Cache", "故障排查", "面试题"]
mainCategory: 测试题
subCategory: 软件
description: 调试与故障案例测试题：HardFault 分析方法、栈溢出检测、FreeRTOS 中断优先级配置、ADC 噪声排查。
---
﻿> **定位**：调试和故障排查是工程师的核心能力，本文件汇总了真实工程中常见的"玄学问题"，所有案例均来自实际项目�?
> **互联**：本文件是前�?7 个文件的知识收束，每道案例都涉及多个知识域的交叉�?
> **难度分布**：L1 ~20% / L2 ~60% / L3 ~20%

### Q02 �?HardFault_Handler 中如何自动抓取栈�?

**📐 L2　💡 高频：是　🔗 关联�?8-Q01**

**答案**�?
```c
__attribute__((naked)) void HardFault_Handler(void)
{
    __asm volatile(
        "tst lr, #4        \n"  // 检�?EXC_RETURN �?bit2
        "ite eq             \n"
        "mrseq r0, msp      \n"  // bit2=0，使�?MSP
        "mrsne r0, psp      \n"  // bit2=1，使�?PSP
        "b hard_fault_handler_c \n"
    );
}

void hard_fault_handler_c(uint32_t *stack_frame)
{
    uint32_t r0  = stack_frame[0];
    uint32_t r1  = stack_frame[1];
    uint32_t r2  = stack_frame[2];
    uint32_t r3  = stack_frame[3];
    uint32_t r12 = stack_frame[4];
    uint32_t lr  = stack_frame[5];   // Link Register
    uint32_t pc  = stack_frame[6];   // Program Counter �?导致 Fault 的地址
    uint32_t psr = stack_frame[7];
    
    // 读取 Fault 状态寄存器
    uint32_t cfsr = SCB->CFSR;
    uint32_t hfsr = SCB->HFSR;
    uint32_t mmfar = SCB->MMFAR;
    uint32_t bfar = SCB->BFAR;
    
    // 记录故障信息（可写入 Flash �?UART 输出�?
    fault_log(FAULT_HARD, pc, cfsr, hfsr);
    
    // 系统复位或进入安全状�?
    HAL_NVIC_SystemReset();
    while(1);
}
```

### Q04 �?Fault 记录�?Flash 实现黑匣�?

**📐 L3　💡 高频：否　🔗 关联�?8-Q01�?2-Q16**

**场景**：你的设备在客户现场偶发复位，需要记录复位原因以便分析�?

**答案**�?
```c
// �?Flash 保留一个扇区用于故障记�?
#define FAULT_LOG_ADDR  0x0800F000  // 倒数第二个扇�?

typedef struct {
    uint32_t magic;        // 0xFAULTRCD
    uint32_t fault_type;
    uint32_t pc;
    uint32_t lr;
    uint32_t cfsr;
    uint32_t reset_count;
} FaultLog_t;

void fault_record(uint32_t type, uint32_t pc, uint32_t lr)
{
    FaultLog_t log;
    log.magic = 0xFAULTRCD;
    log.fault_type = type;
    log.pc = pc;
    log.lr = lr;
    log.cfsr = SCB->CFSR;
    
    HAL_FLASH_Unlock();
    // 写入 Flash（需要预先擦除）
    
    // 写入 LPD 操作...
    HAL_FLASH_Lock();
}
```

### Q06 �?非对齐访问导致的 M0 HardFault

**📐 L2　💡 高频：否　🔗 关联�?1-Q11�?4-Q14**

**场景**：你�?STM32F1 的代码移植到 STM32L0（M0+），发现 HardFault�?

**答案**�?
```
原因：M0/M0+ 不支持非对齐访问
代码中可能有�?
- uint16_t val = *(uint16_t*)(buf+1);  // 奇数地址
- uint32_t val = *(uint32_t*)(buf+1);  // �?4 字节对齐

解决�?
1. 使用 memcpy 替代直接指针访问
2. 确保 DMA 缓冲区和外设数据宽度匹配
3. 使用 __packed 时要格外小心
4. 可以使用 SCB->CFSR �?UNALIGN_TRP 位来检�?
```

### Q08 �?ADC 采样值跳变的排查

**📐 L2　💡 高频：否　🔗 关联�?4-Q08�?4-Q10**

**场景**：你�?ADC 采样值偶尔跳变很大�?

**答案**�?
```
排查步骤�?
1. 检查参考电压是否稳定（VREF+ 引脚上的电容�?
2. 检查采样时间是否足够（高阻抗源需要更长采样时间）
3. 检�?PCB 布局（模拟信号远离开关信号）
4. 检查电源纹波（LDO 输出是否有足够去耦）
5. 检�?ADC 时钟是否超过最大允许值（36MHz for 12-bit�?
6. 检�?DMA 缓冲区是否有 Cache 一致性问题（M7�?

软件改善�?
1. 使用平均滤波（滑动平均或中值滤波）
2. 开�?ADC 硬件过采�?
3. �?ADC 输入前添�?RC 低通滤�?
```

### Q10 �?使用 DWT 周期计数器进行精确测�?

**📐 L2　💡 高频：否　🔗 关联�?2-Q02**

**场景**：你需要测量一段代码的执行时间�?

**答案**�?
```c
// 启用 DWT 周期计数�?
void dwt_init(void)
{
    CoreDebug->DEMCR |= CoreDebug_DEMCR_TRCENA_Msk;
    DWT->CYCCNT = 0;
    DWT->CTRL |= DWT_CTRL_CYCCNTENA_Msk;
}

// 测量代码执行时间
void measure_function(void)
{
    uint32_t start = DWT->CYCCNT;
    
    // 被测代码
    HAL_UART_Transmit(&huart1, data, len, 1000);
    
    uint32_t cycles = DWT->CYCCNT - start;
    float us = (float)cycles / (SystemCoreClock / 1000000.0f);
    printf("Execution time: %.2f us\n", us);
}
```

## 四、优先级翻转故障

### Q12 �?优先级翻转导致系统卡�?

**📐 L2　💡 高频：是　🔗 关联�?5-Q12�?5-Q06**

**场景**：你的三个任务按优先级分工，但高优先级任务偶尔超时�?

**答案**�?
```
故障现象�?
- 高优先级任务 H（电机控制，优先�?4�?
- 中优先级任务 M（数据处理，优先�?3�?
- 低优先级任务 L（传感器采集，优先级 2�?

L 获取 SPI 互斥�?�?M 抢占 L �?H 请求 SPI 互斥量被阻塞
�?M 长时间运�?�?H 超时未执�?�?电机故障

根因�?
1. 使用二值信号量保护 SPI �?没有优先级继�?
2. L 的优先级低于 M，M 可以无限抢占 L
3. H 被间接阻�?

解决�?
1. 将二值信号量替换为互斥量（带优先级继承）
2. 或提�?L 的优先级到不低于 H
3. 或拆�?SPI 访问，减�?L 持有互斥量的时间
4. 使用二值信号量 + 临时优先级提升（手动实现优先级继承）
```

## 五、时序相关故�?

### Q14 �?关键段与中断延迟导致�?PWM 抖动

**📐 L2　💡 高频：否　🔗 关联�?3-Q04�?4-Q05**

**场景**：你的电机控�?PWM 周期偶尔跳动�?

**答案**�?
```
原因：在更新 PWM 寄存器（ARR/CCR）时，被高优先级中断打断
导致更新时机不一致�?

解决�?
1. 在更�?PWM 寄存器时短暂关中断（不是整个系统�?
2. 使用定时器的预装载（Preload）功能，在下一周期生效
3. 使用 DMA 批量更新 PWM 寄存�?

// 使用预装载：在更新事件时同步写入
TIM2->CR1 |= TIM_CR1_ARPE;  // ARR 预装载使�?
TIM2->CCMR1 |= TIM_CCMR1_OC1PE;  // CCR 预装载使�?
// 写入 ARR/CCR 后，在下一周期生效，不被中断打�?
```

### Q16 �?中断丢失导致的按键误触发

**📐 L2　💡 高频：否　🔗 关联�?3-Q06�?4-Q01**

**场景**：你的按键在中断中检测，偶尔一次按下被识别为多次�?

**答案**�?
```
原因�?
- 按键抖动导致多次上升�?下降沿触�?
- 或中断优先级太低，被打断后又重新进入

解决�?
1. 硬件去抖：RC 滤波电路
2. 软件去抖：在中断中启动定时器，延�?20ms 后检�?
3. 使用状态机：在中断中设置标志，在任务中延时检�?

// 中使用状态机：在中断中设置标�?
void EXTI0_IRQHandler(void)
{
    __HAL_GPIO_EXTI_CLEAR_IT(GPIO_PIN_0);
    
    // 设置标志，不直接处理
    button_event = 1;
    button_tick = HAL_GetTick();
}

// 在任务中检测（去抖�?
void button_task(void *param)
{
    while (1) {
        if (button_event && (HAL_GetTick() - button_tick > 20)) {
            button_event = 0;
            if (HAL_GPIO_ReadPin(BUTTON_GPIO_Port, BUTTON_Pin) == GPIO_PIN_SET) {
                // 确认按键按下
                handle_button_press();
            }
        }
        vTaskDelay(pdMS_TO_TICKS(5));
    }
}
```

### Q18 �?长时间运行后内存泄漏导致死机

**📐 L3　💡 高频：否　🔗 关联�?1-Q10�?5-Q14**

**场景**：你的设备运�?72 小时后死机�?

**答案**�?
```
排查�?
1. 检�?FreeRTOS �?uxGetFreeHeapSize() �?mallinfo()
2. 在每�?pvPortMalloc()/vPortFree() 中记�?
3. 检查是否有只分配不释放的场�?

常见原因�?
- 在任务中动态分配内存但忘记释放
- 使用第三方库（如 JSON 解析）后没有调用清理函数
- 在中断中调用 pvPortMalloc（不允许�?

解决�?
1. 使用静态分配替代动态分�?
2. 如果必须动态分配，记录分配/释放日志
3. 使用内存池（memory pool）替�?malloc
4. 启用 FreeRTOS �?trace 功能（traceMALLOC/traceFREE�?
```

### Q20 �?调试工具链总结

**📐 L1　💡 高频：否**

**答案**�?

| 工具 | 用�?| 适用场景 |
|||> **本文件总结**：调试能�?= 知识深度 × 工具熟练�?× 方法论。重点掌握：HardFault 分析方法、栈溢出检测、优先级翻转排查、Cache 一致性、系统化调试思路�?
