---
title: 嵌入式面试测试题 �?中断�?DMA
slug: embedded-test-interrupt-dma
date: 2026-07-13
tags: ["中断", "NVIC", "DMA", "优先级", "嵌套", "Cache一致性", "面试题"]
mainCategory: 测试题
subCategory: 软件
description: 中断与 DMA 测试题：NVIC 优先级分组、中断嵌套与双栈、DMA 三种模式、半传输中断、Cache 一致性。
---
﻿> **定位**：中断和 DMA 是嵌入式系统软件与硬件交互的核心机制，也是面试中区分"会用�?�?懂原�?的关键�?
> **互联**�?1#Q07（volatile）→ 本文件（ISR 共享变量）；02#Q12（向量表）→ 本文件（NVIC）；04-外设 �?本文件（外设中断）；05-RTOS �?本文件（临界区）
> **难度分布**：L1 ~40% / L2 ~50% / L3 ~10%

### Q02 �?中断优先级分组与 FreeRTOS 的冲�?

**📐 L2　💡 高频：是　🔗 关联�?3#Q01�?5#Q10**

**场景**：FreeRTOS �?`configLIBRARY_LOWEST_INTERRUPT_PRIORITY` 配置错误，导致系统崩溃�?

**答案**�?
- FreeRTOS 只能管理一定优先级范围内的中断
- 高于此范围的中断（如最高优先级中断）不�?FreeRTOS 管理，不能调�?FreeRTOS API

```c
// FreeRTOSConfig.h
#define configLIBRARY_LOWEST_INTERRUPT_PRIORITY      15
#define configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY 5
// 优先�?6-15 的中断可以调�?FreeRTOS API（FromISR 版本�?
// 优先�?0-5 的中断不�?FreeRTOS 管理，不能调用任�?FreeRTOS 功能
```

**工程实践**：将系统 Tick �?PendSV 设置为最低优先级�?5），确保上下文切换不会屏蔽其他中断�?

### Q04 �?中断延迟与零延迟中断

**📐 L2　💡 高频：否　🔗 关联�?3#Q01�?3#Q03**

**场景**：你的电机控制中断需要在 1μs 内响应，�?FreeRTOS 的临界区引入了延迟�?

**答案**�?
- 中断延迟 = 从信号到达至 ISR 第一条指令执行的时间
- Cortex-M 硬件中断延迟�?2 个时钟周期（无等待状态）
- 软件延迟来源�?
  - `__disable_irq()` 关中断的临界�?
  - Flash 等待周期
  - Cache 未命中（M7�?
  
```c
// FreeRTOS 临界区类型对�?
taskENTER_CRITICAL();       // 关中断（最大延迟）
taskENTER_CRITICAL_FROM_ISR();  // 从中断中进入
vTaskSuspendAll();          // 仅调度器挂起（中断仍可触发，延迟更低�?
```

### Q06 �?外部中断（EXTI）与 GPIO 中断

**📐 L1　💡 高频：是　🔗 关联�?4#Q01（GPIO�?*

**场景**：你需要通过按键触发一个外部中断来处理低功耗唤醒�?

**答案**�?
```c
// GPIO 中断配置
GPIO_InitTypeDef GPIO_InitStruct = {0};
GPIO_InitStruct.Pin = GPIO_PIN_0;
GPIO_InitStruct.Mode = GPIO_MODE_IT_RISING;  // 上升沿触�?
GPIO_InitStruct.Pull = GPIO_NOPULL;
HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);

// 使能中断
HAL_NVIC_SetPriority(EXTI0_IRQn, 6, 0);
HAL_NVIC_EnableIRQ(EXTI0_IRQn);

// ISR
void EXTI0_IRQHandler(void)
{
    __HAL_GPIO_EXTI_CLEAR_IT(GPIO_PIN_0);
    // 处理逻辑
    button_pressed = 1;  // volatile 标志（需在外部定�?volatile uint8_t button_pressed�?
}

// 注意：EXTI 是每个引脚一根线，EXTI0 对应所�?GPIO �?Pin0
// �?ISR 中需要判断是哪个端口（通常设计中一�?Pin 只接一个端口）
```

## 二、DMA 原理

### Q08 �?DMA 的三种传输模�?

**📐 L1　💡 高频：是　🔗 关联�?4#Q15（ADC+DMA）�?6#Q01（UART+DMA�?*

**场景**：你需要选择 DMA 模式来采�?ADC 数据�?

**答案**�?

| 模式 | 特点 | 适用场景 |
|||### Q09 �?DMA 传输完成中断与半传输中断

**📐 L2　💡 高频：是　🔗 关联�?3#Q08�?4#Q15**

**场景**：你需要在 DMA 传一半时就开始处理数据，同时 DMA 继续填充另一半�?

**答案**�?
```c
// 使能半传输完成中断和传输完成中断
HAL_ADC_Start_DMA(&hadc1, (uint32_t*)adc_buf, ADC_BUF_SIZE);

void DMA2_Stream0_IRQHandler(void)
{
    // 半传输完成：前半缓冲区已�?
    if (__HAL_DMA_GET_IT_SOURCE(&hdma_adc1, DMA_IT_HT)) {
        process_buffer(&adc_buf[0], ADC_BUF_SIZE / 2);
    }
    // 传输完成：后半缓冲区已满
    if (__HAL_DMA_GET_IT_SOURCE(&hdma_adc1, DMA_IT_TC)) {
        process_buffer(&adc_buf[ADC_BUF_SIZE / 2], ADC_BUF_SIZE / 2);
    }
}
```

### Q11 �?DMA 仲裁与优先级

**📐 L2　💡 高频：否　🔗 关联�?2#Q04（总线矩阵�?*

**场景**：你�?DMA 传输偶尔丢失数据（如 ADC 溢出）�?

**答案**�?
- 当多�?DMA 通道同时请求时，硬件优先级（通道号）决定顺序
- 可通过软件配置 DMA 通道优先级（Low / Medium / High / Very High�?
- �?DMA �?CPU 冲突时，总线矩阵轮询仲裁

```c
// 配置 DMA 优先�?
DMA_HandleTypeDef hdma;
hdma.Init.Priority = DMA_PRIORITY_VERY_HIGH;
HAL_DMA_Init(&hdma);
```

## 三、中断与 DMA 的协同设�?

### Q13 �?UART DMA 接收 + IDLE 中断检测帧

**📐 L2　💡 高频：是　🔗 关联�?3#Q08�?6#Q01**

**场景**：你需要接收变�?UART 帧，不知道何时数据结束�?

**答案**�?
```c
// UART DMA 接收 + IDLE 中断方案
// 1. 启动 DMA 循环接收
HAL_UART_Receive_DMA(&huart1, rx_buf, RX_BUF_SIZE);

// 2. 使能 IDLE 中断
__HAL_UART_ENABLE_IT(&huart1, UART_IT_IDLE);

// 3. �?UART 中断中检�?IDLE
void USART1_IRQHandler(void)
{
    if (__HAL_UART_GET_FLAG(&huart1, UART_FLAG_IDLE)) {
        __HAL_UART_CLEAR_IDLEFLAG(&huart1);
        
        // 计算接收到的数据长度
        uint16_t dma_remaining = __HAL_DMA_GET_COUNTER(huart1.hdmarx);
        uint16_t rx_len = RX_BUF_SIZE - dma_remaining;
        
        // 通知任务处理
        rx_frame_ready = 1;
        rx_frame_len = rx_len;
    }
}
```

**面试追问**：为什么不直接�?DMA 传输完成中断中处理？
> 因为不知道帧何时结束。DMA 传输完成意味着缓冲区满，�?IDLE 表示线路空闲超过一个帧时间，更适合检测帧边界�?

### Q15 �?SPI DMA 传输的时序问�?

**📐 L2　💡 高频：是　🔗 关联�?4#Q16（SPI 通信�?*

**场景**：你�?HAL_SPI_TransmitReceive_DMA() 时，发现数据错位或丢失�?

**答案**�?
- SPI 是全双工，TX �?RX 必须同时启动
- DMA �?TX �?RX 通道必须正确配置
- 使用 `HAL_SPI_TransmitReceive_DMA()` 而非分别调用 TX �?RX

```c
// �?正确：同时启�?TX �?RX
HAL_SPI_TransmitReceive_DMA(&hspi1, tx_buf, rx_buf, len);

// �?错误：分别调用可能导致时序错�?
HAL_SPI_Transmit_DMA(&hspi1, tx_buf, len);
HAL_SPI_Receive_DMA(&hspi1, rx_buf, len);  // TX 可能还没完成
```

### Q17 �?优先级翻转问题的硬件层面分析

**📐 L3　💡 高频：否　🔗 关联�?5#Q12�?8#Q10**

**场景**：高优先级中断被低优先级任务阻塞（通过互斥量）�?

**答案**�?
```
低优先级任务 L：获取互斥量 M
中优先级任务 M：抢�?L（因�?M 优先级高�?
高优先级任务 H：需要互斥量 M，被阻塞
�?M 一直运行，L 无法释放 M，H 无限等待

解决方案：优先级继承（Priority Inheritance�?
�?H 请求 L 持有�?M 时，L 的优先级临时提升�?H 的优先级
L 执行完毕释放 M 后恢复原优先�?
```

---

> **本文件总结**：中断和 DMA �?系统响应能力"的双刃剑。重点掌握：NVIC 优先级分组、DMA 三种模式、Cache 一致性、IDLE 中断帧检测、中断延迟处理�?
