---
title: 嵌入式面试测试题 �?外设编程实战
slug: embedded-test-peripheral-programming
date: 2026-07-13
tags: ["GPIO", "定时器", "PWM", "ADC", "SPI", "I2C", "UART", "USB", "面试题"]
mainCategory: 测试题
subCategory: 软件
description: 外设编程实战测试题：GPIO 模式、定时器时基、PWM 输出、ADC 采样、SPI/I2C 模式、UART 错误处理、USB 基础。
---
﻿> **定位**：外设编程是嵌入式工程师的日常，面试中常�?你如何配�?调试某个外设"来考察实际能力�?
> **互联**�?1#Q13（位操作）→ 本文件（寄存器配置）�?3#Q08（DMA）→ 本文件（ADC+DMA/SPI+DMA）；02#Q07（时钟树）→ 本文件（定时器时钟）
> **难度分布**：L1 ~50% / L2 ~40% / L3 ~10%

|
| 输入浮空 | 无上�?下降，电平由外部决定 | 按键（外部上拉） |
| 输入上拉 | 默认高电�?| 按键（低电平有效�?|
| 输入下拉 | 默认低电�?| 按键（高电平有效�?|
| 模拟输入 | 数字功能关闭，直接连 ADC | ADC 采样 |
| 推挽输出 | 可驱动高/�?| LED、数字信�?|
| 开漏输�?| 只能拉低，需外部上拉 | I2C、线与逻辑 |
| 复用推挽 | 外设控制输出 | UART TX、SPI MOSI |
| 复用开�?| 外设控制开�?| I2C SCL/SDA |

```c
// I2C 引脚配置（开�?+ 复用�?
GPIO_InitTypeDef GPIO_InitStruct = {0};
GPIO_InitStruct.Pin = GPIO_PIN_6 | GPIO_PIN_7;  // SCL + SDA
GPIO_InitStruct.Mode = GPIO_MODE_AF_OD;         // 复用开�?
GPIO_InitStruct.Pull = GPIO_PULLUP;             // 外部或内部上�?
GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_HIGH;
GPIO_InitStruct.Alternate = GPIO_AF4_I2C1;
HAL_GPIO_Init(GPIOB, &GPIO_InitStruct);
```

### Q03 �?GPIO 锁定机制

**📐 L2　💡 高频：否　🔗 关联�?1#Q14**

**场景**：你配置好关键引脚（�?PWM 输出）后，防止后续代码误修改�?

**答案**�?
```c
// 配置后锁定引�?
HAL_GPIO_LockPin(GPIOA, GPIO_PIN_5);
// 锁定后，直到下次复位，无法修�?PA5 的配�?
// 适用于：配置安全关键的输出引脚（如电机使能信号）
```

### Q05 �?PWM 输出配置

**📐 L1　💡 高频：是　🔗 关联�?4#Q04**

**场景**：你需要产生一个频�?20kHz、占空比 30% �?PWM 信号驱动电机�?

**答案**�?
```c
// 目标�?0kHz PWM，占空比 30%
// 定时器时�?84MHz（APB1 定时器）
// PSC = 0, ARR = 4199 �?84MHz / 4200 = 20kHz
// CCR = 1260 �?30% 占空�?

TIM_OC_InitTypeDef sConfigOC = {0};
sConfigOC.OCMode = TIM_OCMODE_PWM1;
sConfigOC.Pulse = 1260;              // 占空�?30%
sConfigOC.OCPolarity = TIM_OCPOLARITY_HIGH;
sConfigOC.OCFastMode = TIM_OCFAST_DISABLE;
HAL_TIM_PWM_ConfigChannel(&htim3, &sConfigOC, TIM_CHANNEL_1);

HAL_TIM_PWM_Start(&htim3, TIM_CHANNEL_1);
```

**面试追问**：PWM1 �?PWM2 模式的区别？
> PWM1：计数器 < CCR 时输出有效电平，>= CCR 时输出无效电平�?
> PWM2：与 PWM1 相反�?
> 对于高有效极性，PWM1 的占空比 = CCR / ARR�?

### Q07 �?编码器模�?

**📐 L2　💡 高频：否　🔗 关联�?4#Q04**

**场景**：你需要读取正交编码器的位置�?

**答案**�?
```c
TIM_Encoder_InitTypeDef sConfig = {0};
sConfig.EncoderMode = TIM_ENCODERMODE_TI12;  // 4 倍频
sConfig.IC1Polarity = TIM_ICPOLARITY_RISING;
sConfig.IC1Selection = TIM_ICSELECTION_DIRECTTI;
sConfig.IC1Prescaler = TIM_ICPSC_DIV1;
sConfig.IC1Filter = 0x0F;  // 滤波
sConfig.IC2Polarity = TIM_ICPOLARITY_RISING;
sConfig.IC2Selection = TIM_ICSELECTION_DIRECTTI;
sConfig.IC2Prescaler = TIM_ICPSC_DIV1;
sConfig.IC2Filter = 0x0F;
HAL_TIM_Encoder_Init(&htim3, &sConfig);
HAL_TIM_Encoder_Start(&htim3, TIM_CHANNEL_ALL);

// 读取位置
int16_t count = (int16_t)__HAL_TIM_GET_COUNTER(&htim3);
```

### Q09 �?ADC 校准

**📐 L1　💡 高频：否　🔗 关联�?4#Q08**

**场景**：你�?ADC 读数有固定偏移�?

**答案**�?
```c
// 上电后执行一次校�?
HAL_ADCEx_Calibration_Start(&hadc1);

// 或使用内部参考电压校�?
// Vrefint 的工厂校准值存储在 0x1FFF_7A2A（STM32F4�?
```

### Q11 �?ADC 过采样提高分辨率

**📐 L2　💡 高频：否　🔗 关联�?4#Q08**

**场景**：你�?12 �?ADC 不够用，需要提高分辨率�?

**答案**�?
- STM32F4 �?ADC 硬件支持过采样（最�?256×），可将 12 位提升到 16 �?
- 软件过采样：采样 N 次取平均，每 4 倍采样提�?1 �?

```c
// 硬件过采样（STM32F4/H7 支持�?
hadc1.Init.OversamplingMode = ENABLE;
hadc1.Init.Oversampling.Ratio = ADC_OVERSAMPLING_RATIO_256;
hadc1.Init.Oversampling.RightBitShift = ADC_RIGHTBITSHIFT_4;  // 右移 4 �?
hadc1.Init.Oversampling.TriggeredMode = ADC_TRIGGEREDMODE_SINGLE_TRIGGER;
```

### Q13 �?UART 各种错误的处�?

**📐 L2　💡 高频：是　🔗 关联�?6#Q02（Modbus 错误处理�?*

**场景**：你�?UART 偶尔出现乱码或数据丢失�?

**答案**�?
```c
void HAL_UART_ErrorCallback(UART_HandleTypeDef *huart)
{
    uint32_t error = HAL_UART_GetError(huart);
    
    if (error & HAL_UART_ERROR_PE) {
        // 奇偶校验错误：检查波特率和校验位配置
    }
    if (error & HAL_UART_ERROR_NE) {
        // 噪声错误：可能波特率不匹配或线路干扰
    }
    if (error & HAL_UART_ERROR_FE) {
        // 帧错误：通常波特率不匹配
    }
    if (error & HAL_UART_ERROR_ORE) {
        // 过载错误：UART 还没处理完新数据就到�?
        // 原因：中断处理太慢或 DMA 未启�?
    }
    
    // 清除错误并重新接�?
    __HAL_UART_CLEAR_OREFLAG(huart);
    HAL_UART_Receive_DMA(huart, rx_buf, RX_BUF_SIZE);
}
```

### Q15 �?I2C �?ACK/NACK 机制与常见问�?

**📐 L2　💡 高频：是　🔗 关联�?4#Q01**

**场景**：你�?I2C 通信偶尔超时，SCL 被拉低�?

**答案**�?
```
I2C 常见问题�?
1. 设备不拉 ACK：地址错误、设备忙、设备未就绪
2. SCL 被拉低（时钟拉伸）：设备需要更多时间处理数�?
3. SDA 被拉低：总线死锁，需要发�?9 个时钟脉冲恢�?

I2C ACK/NACK 协议机制�?
- 每传�?1 字节�? 个时钟）后，�?9 个时钟周期用于应�?
- 接收方在�?9 个时钟拉�?SDA �?ACK（确认接收成功）
- 接收方在�?9 个时钟释�?SDA（高电平）→ NACK（通知发送方停止�?
- 主机作为接收方时，由主机在第 9 个时钟产�?ACK/NACK
- 从机作为接收方时，由从机在第 9 个时钟产�?ACK/NACK
- STM32 �?I2C 外设自动处理�?9 个时钟的 ACK/NACK 时序
- HAL 库的超时机制（如 100ms）会检�?SCL 被从机拉低是否超过阈�?

HAL 库的 I2C 超时处理�?
HAL_I2C_Mem_Read(&hi2c1, addr, reg, 1, buf, len, 100);
// 如果超时，返�?HAL_TIMEOUT
```

```c
// 恢复 I2C 总线死锁
void i2c_bus_recovery(void)
{
    // �?SCL 切换�?GPIO 开漏输�?
    HAL_GPIO_DeInit(GPIOB, GPIO_PIN_6);
    GPIO_InitTypeDef GPIO_InitStruct = {0};
    GPIO_InitStruct.Pin = GPIO_PIN_6;
    GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_OD;
    HAL_GPIO_Init(GPIOB, &GPIO_InitStruct);
    
    // 发�?9 个时钟脉�?
    for (int i = 0; i < 9; i++) {
        HAL_GPIO_WritePin(GPIOB, GPIO_PIN_6, GPIO_PIN_SET);
        HAL_Delay(1);
        HAL_GPIO_WritePin(GPIOB, GPIO_PIN_6, GPIO_PIN_RESET);
        HAL_Delay(1);
    }
    
    // 重新初始�?I2C
    HAL_I2C_Init(&hi2c1);
}
```

### Q17 �?定时器同步与触发输出

**📐 L3　💡 高频：否　🔗 关联�?4#Q04�?4#Q05**

**场景**：你需要两个定时器同步启动，一个触发另一个�?

**答案**�?
```c
// TIM2 作为主定时器，TIM3 作为从定时器
// TIM2 的更新事件触�?TIM3 启动

// 主定时器配置
TIM_MasterConfigTypeDef sMasterConfig = {0};
sMasterConfig.MasterOutputTrigger = TIM_TRGO_UPDATE;
sMasterConfig.MasterSlaveMode = TIM_MASTERSLAVEMODE_ENABLE;
HAL_TIMEx_MasterConfigSynchronization(&htim2, &sMasterConfig);

// 从定时器配置
TIM_SlaveConfigTypeDef sSlaveConfig = {0};
sSlaveConfig.SlaveMode = TIM_SLAVEMODE_TRIGGER;
sSlaveConfig.InputTrigger = TIM_TS_ITR1;  // ITR1 = TIM2 �?TRGO
sSlaveConfig.TriggerPolarity = TIM_TRIGGERPOLARITY_RISING;
HAL_TIMEx_SlaveConfigSynchronization(&htim3, &sSlaveConfig);
```

### Q19 �?USB 基础（CDC / HID / MSC�?

**📐 L2　💡 高频：否　🔗 关联�?3-Q08（USB DMA）�?6-Q01（CDC 虚拟串口�?*

**场景**：你需要通过 USB �?STM32 模拟为一个串口设备（CDC）与 PC 通信�?

**答案**�?
```
USB（Universal Serial Bus）基础�?
- 设备类型�?
  - CDC（Communication Device Class）：虚拟串口
  - HID（Human Interface Device）：键盘/鼠标/游戏�?
  - MSC（Mass Storage Class）：U �?
  - Composite：组合设�?

- USB 描述符：
  - 设备描述符：VID、PID、设备类
  - 配置描述符：接口、端�?
  - 接口描述符：类、子类、协�?
  - 端点描述符：方向、传输类型、最大包�?

- 传输类型�?
  - Control（控制）：枚举和配置
  - Bulk（批量）：大容量数据（MSC、CDC 数据�?
  - Interrupt（中断）：定期小数据（HID�?
  - Isochronous（等时）：实时流（音�?视频�?

STM32 USB 硬件�?
- 内置 USB FS�?2Mbps）或 USB HS�?80Mbps，需外部 PHY�?
- 使用 HSI48 �?PLL 提供 48MHz USB 时钟
- 内置 USB 上拉电阻（部分型号）
```

```c
// USB CDC 接收回调（在 USB 中断中调用）
static int8_t CDC_Receive_FS(uint8_t* Buf, uint32_t *Len)
{
    // 接收到的数据�?Buf 中，长度�?*Len
    // 将数据存入环形缓�?
    for (uint32_t i = 0; i < *Len; i++) {
        ring_buf_write(&usb_rx_buf, Buf[i]);
    }
    // 重新准备接收
    CDC_Receive_FS(Buf, Len);
    return USBD_OK;
}

// USB CDC 发�?
void usb_send(uint8_t *data, uint16_t len)
{
    CDC_Transmit_FS(data, len);
}
```

---

> **本文件总结**：外设编程的核心�?理解时序"。重点掌握：定时器时基计算、PWM 配置、ADC+DMA 多通道扫描、UART 错误处理、SPI/I2C 模式选择、USB 基础�?
