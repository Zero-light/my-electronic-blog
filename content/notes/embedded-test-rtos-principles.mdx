---
title: 嵌入式面试测试题 �?RTOS 核心原理
slug: embedded-test-rtos-principles
date: 2026-07-13
tags: ["FreeRTOS", "任务调度", "信号量", "互斥量", "消息队列", "优先级翻转", "面试题"]
mainCategory: 测试题
subCategory: 软件
description: RTOS 核心原理测试题（FreeRTOS）：任务状态与调度、二值信号量 vs 互斥量、优先级翻转与继承、消息队列、栈溢出检测。
---
﻿> **定位**：RTOS 是嵌入式中高层岗位的分水岭，理解任务调度、同步机制、内存管理是必备能力�?
> **互联**�?1-C 语言 �?本文件（内存管理）；02-中断 �?本文件（临界区）�?3-DMA �?本文件（异步通信�?
> **难度分布**：L1 ~50% / L2 ~40% / L3 ~10%

### Q02 �?任务的四种状�?

**📐 L1　💡 高频：是　🔗 关联�?5#Q03�?5#Q04**

**场景**：你的任务在某些时刻不运行，你需要分析它的状态�?

**答案**�?

```
运行�?(Running)：正在占�?CPU
    �?时间片用�?被抢�?主动让出
就绪�?(Ready)：已经准备好运行，等待调�?
    �?调用阻塞 API（如 xQueueReceive�?
阻塞�?(Blocked)：等待某个事件（时间/信号�?队列�?
    �?vTaskSuspend()
挂起�?(Suspended)：被显式挂起，无法被唤醒（只能通过 vTaskResume�?
```

### Q04 �?空闲任务（Idle Task�?

**📐 L1　💡 高频：是　🔗 关联�?5#Q02�?7#Q01**

**场景**：当所有任务都阻塞时，CPU 在做什么？

**答案**�?
- 空闲任务是最低优先级任务（优先级 0），当没有其他任务可运行时执�?
- 空闲任务中执行低功耗模式（WFI/WFE�?
- 可以注册空闲钩子函数执行后台工作

```c
void vApplicationIdleHook(void)
{
    // 低功耗：进入 Sleep 模式
    __asm volatile("wfi");
    
    // 后台工作：垃圾回收、看门狗喂狗�?
}

// 空闲任务的栈大小
#define configMINIMAL_STACK_SIZE  128  // 单位：字
```

|-|
| 速度 | 快（无需额外对象�?| 稍慢 |
| 内存 | 无额外开销（在 TCB 中） | 需要额外的信号量对�?|
| 方向 | 1 �?1（发送到一个任务） | 1 �?N（任何任务都可获取） |
| 数据 | 可携带一�?32 位�?| 无数�?|
| 覆盖 | 可选择覆盖或累�?| 只能 Give 一�?|

```c
// 使用任务通知�?ISR 通知任务
void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)
{
    vTaskNotifyGiveFromISR(huart_task_handle, NULL);
}

// 任务中等待通知
void uart_task(void *param)
{
    while (1) {
        ulTaskNotifyTake(pdTRUE, portMAX_DELAY);
        // 处理 UART 数据
    }
}
```

--|--|
| 初始�?| 0（先 Take �?Give�?| 1（先可用�?|
| 优先级继�?| �?�?| �?有（防优先级翻转�?|
| 递归获取 | �?不支�?| �?支持（Recursive Mutex�?|
| 用�?| 任务�?ISR 同步 | 共享资源保护 |

```c
// 互斥量保�?SPI 总线
SemaphoreHandle_t spi_mutex;

void spi_transmit(uint8_t *data, uint16_t len)
{
    if (xSemaphoreTake(spi_mutex, pdMS_TO_TICKS(100)) == pdTRUE) {
        HAL_SPI_Transmit_DMA(&hspi1, data, len);
        // 等待 DMA 完成
        ulTaskNotifyTake(pdTRUE, portMAX_DELAY);
        xSemaphoreGive(spi_mutex);
    }
}
```

### Q08 �?事件组（Event Group�?

**📐 L2　💡 高频：否　🔗 关联�?5#Q05**

**场景**：你需要等待多个事件中的任意一个发生�?

**答案**�?
```c
#define EVENT_UART_READY  (1 << 0)
#define EVENT_CAN_RX      (1 << 1)
#define EVENT_ADC_DONE    (1 << 2)

EventGroupHandle_t events = xEventGroupCreate();

// ISR 中设置事�?
xEventGroupSetBitsFromISR(events, EVENT_UART_READY, NULL);

// 任务中等�?
EventBits_t bits = xEventGroupWaitBits(
    events,
    EVENT_UART_READY | EVENT_CAN_RX,  // 等待的事�?
    pdTRUE,                            // 清除�?
    pdFALSE,                           // 等待任意一个（非全部）
    pdMS_TO_TICKS(100)                 // 超时
);

if (bits & EVENT_UART_READY) {
    // 处理 UART
}
```

## 三、临界区与中�?

### Q10 �?进入临界区的三种方式

**📐 L1　💡 高频：是　🔗 关联�?1#Q07�?3#Q01**

**场景**：你需要保护一段代码不被中断打断�?

**答案**�?

| 方式 | API | 效果 | 适用场景 |
|||
| 关中�?| `taskENTER_CRITICAL()` | 屏蔽所有可管理中断 | 极短临界区（几条指令�?|
| 挂起调度�?| `vTaskSuspendAll()` | 不关中断，禁止调�?| 稍长的操作（可能被中断触发） |
| 互斥�?| `xSemaphoreTake()` | 仅屏蔽同优先级任�?| 仅用于任务间，ISR 不能获取 |

```c
// �?错误：ISR 中获取互斥量
void HAL_UART_TxCpltCallback(UART_HandleTypeDef *huart)
{
    xSemaphoreTake(mutex, 0);  // 错误！ISR 不能阻塞
}

// �?正确：ISR 中使�?taskENTER_CRITICAL_FROM_ISR()
void HAL_UART_TxCpltCallback(UART_HandleTypeDef *huart)
{
    UBaseType_t uxSavedInterruptStatus = taskENTER_CRITICAL_FROM_ISR();
    shared_counter++;
    taskEXIT_CRITICAL_FROM_ISR(uxSavedInterruptStatus);
}
```

### Q12 �?优先级翻转（Priority Inversion�?

**📐 L2　💡 高频：是　🔗 关联�?5#Q06�?8#Q10**

**场景**：你的高优先级任务被低优先级任务阻塞了很长时间�?

**答案**�?
```
场景�?
- 低优先级任务 L 获取互斥�?M
- 高优先级任务 H 就绪，抢�?L
- H 尝试获取 M，但 M �?L 持有 �?H 阻塞
- 中优先级任务 M 就绪，抢�?L
- L 无法运行，无法释�?M �?H 无限等待

解决方案：优先级继承
- H 请求 M 时，L 的优先级临时提升�?H 的优先级
- L 可以运行，释�?M 后恢复原优先�?
- M 无法抢占提升后的 L

FreeRTOS 实现：使用互斥量（带优先级继承），不用二值信号量
```

## 四、内存管�?

### Q14 �?FreeRTOS 的五种内存分配方�?

**📐 L2　💡 高频：是　🔗 关联�?1#Q10**

**场景**：你需要选择合适的内存管理方案�?

**答案**�?

| 方案 | 特点 | 适用场景 |
|||### Q15 �?栈溢出检�?

**📐 L2　💡 高频：是　🔗 关联�?2#Q14�?8#Q03**

**场景**：你的任务偶�?HardFault，怀疑是栈溢出�?

**答案**�?
```c
// FreeRTOSConfig.h
#define configCHECK_FOR_STACK_OVERFLOW  2  // 方法 2：检查栈底模�?

// 溢出钩子函数
void vApplicationStackOverflowHook(TaskHandle_t xTask, char *pcTaskName)
{
    // 任务栈溢出！记录任务名并重启
    log_error("Stack overflow in task: %s", pcTaskName);
    HAL_NVIC_SystemReset();
}

// 获取任务剩余栈大�?
UBaseType_t remaining = uxTaskGetStackHighWaterMark(task_handle);
printf("Task '%s' remaining: %lu words\n", pcTaskName, remaining);
```

**面试追问**：High Water Mark 是什么？
> 是一个任务从创建到现在，栈指针到达过的最低位置（即最大栈使用量）与栈顶之间的剩余空间。数值越小说明栈使用越接近上限�?

## 五、消息队�?

### Q17 �?消息队列的工作原�?

**📐 L1　💡 高频：是　🔗 关联�?5#Q05�?5#Q06**

**场景**：你需要从一个任务向另一个任务发送数据�?

**答案**�?
```
消息队列结构�?
- 存储区域（环形缓冲区�?
- 头指针（写入位置�?
- 尾指针（读取位置�?
- 队列长度（项目数�?
- 项目大小

操作�?
- xQueueSend()：写入队列（覆盖队尾），队列满时阻塞
- xQueueReceive()：从队列读取（从队首），队列空时阻塞
- xQueueSendFromISR()：从 ISR 写入
```

```c
// 创建队列�?0 个元素，每个 4 字节�?
QueueHandle_t queue = xQueueCreate(10, sizeof(uint32_t));

// 任务 A：发�?
uint32_t data = sensor_read();
xQueueSend(queue, &data, pdMS_TO_TICKS(100));

// 任务 B：接�?
uint32_t received;
xQueueReceive(queue, &received, portMAX_DELAY);
```

## 六、软件定时器

### Q19 �?软件定时�?vs 硬件定时�?

**📐 L2　💡 高频：否　🔗 关联�?4#Q04�?5#Q16**

**场景**：你需要实现一个周期性的数据采集任务�?

**答案**�?

| 特�?| 软件定时�?| 硬件定时�?|
||--|
| 精度 | �?SysTick 和调度延迟影�?| 精确到时钟周�?|
| 回调上下�?| 在定时器守护任务中执�?| 在中断中执行 |
| 资源消�?| 占用 RAM | 占用硬件通道 |
| 最大数�?| 仅受 RAM 限制 | MCU 外设数量限制 |
| 功�?| 需保持 SysTick 运行 | 可独立于 CPU 运行 |

```c
// 创建软件定时�?
TimerHandle_t timer = xTimerCreate(
    "sample",
    pdMS_TO_TICKS(10),  // 10ms 周期
    pdTRUE,             // 自动重载
    NULL,
    sample_callback
);
xTimerStart(timer, 0);
```

## 七、综合进�?

### Q21 �?FreeRTOS 的双栈机制（MSP + PSP�?

**📐 L2　💡 高频：是　🔗 关联�?2#Q20�?5#Q02**

**场景**：你需要理�?FreeRTOS 上下文切换时栈的使用�?

**答案**�?
```
复位后：使用 MSP（主栈指针）
进入第一个任务后�?
  - 任务使用 PSP（进程栈指针�?
  - 中断使用 MSP（主栈指针）

这意味着�?
- 每个任务有独立的栈（通过 PSP 指向�?
- 所有中断共享一个栈（MSP�?
- 中断栈大小需考虑最大嵌套深�?

 FreeRTOS �?Port 层（port.c）：
  - 启动调度器时配置 PSP 指向任务�?
  - 中断时自动使�?MSP
  - PendSV 异常中执行上下文切换
```

### Q23 �?在中断中使用 FreeRTOS API 的注意事�?

**📐 L2　💡 高频：是　🔗 关联�?5#Q10�?5#Q11**

**场景**：你�?ISR 中调�?`xQueueSend()` 而不�?`xQueueSendFromISR()`�?

**答案**�?
```c
// �?错误：在 ISR 中使用非 FromISR �?API
void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)
{
    xQueueSend(queue, &data, 0);  // 错误�?
}

// �?正确
void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart)
{
    BaseType_t xHigherPriorityTaskWoken = pdFALSE;
    xQueueSendFromISR(queue, &data, &xHigherPriorityTaskWoken);
    portYIELD_FROM_ISR(xHigherPriorityTaskWoken);
}
```

---

> **本文件总结**：RTOS 的核心是"资源管理 + 时序保证"。重点掌握：任务优先级与调度、互斥量 vs 信号量、优先级翻转与继承、消息队列、栈溢出检测、临界区选择�?
