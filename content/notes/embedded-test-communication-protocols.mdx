---
title: 嵌入式面试测试题 �?通信协议�?
slug: embedded-test-communication-protocols
date: 2026-07-13
tags: ["UART", "Modbus", "CAN", "MQTT", "Bootloader", "状态机", "面试题"]
mainCategory: 测试题
subCategory: 软件
description: 通信协议栈测试题：UART 状态机、Modbus RTU 协议、CAN 总线基础、MQTT、Bootloader 通信协议设计。
---
﻿> **定位**：通信协议是嵌入式系统与外部世界交互的桥梁，协议设计能力是高级嵌入式工程师的标志�?
> **互联**�?3-Q07（中断延迟处理）�?本文件（UART 协议解析）；04-外设 �?本文件（协议底层）；05-RTOS �?本文件（协议任务设计�?
> **难度分布**：L1 ~40% / L2 ~50% / L3 ~10%

### Q02 �?UART 协议的超时检�?

**📐 L2　💡 高频：否　🔗 关联�?6#Q01�?1#Q30**

**场景**：UART 帧传输中途停止，状态机永远停留�?DATA 状态�?

**答案**�?
```c
// �?1ms 定时器中检测超�?
void rx_timeout_check(RxContext_t *ctx)
{
    if (ctx->state != RX_STATE_HEADER1) {
        if ((HAL_GetTick() - ctx->last_tick) > FRAME_TIMEOUT_MS) {
            ctx->state = RX_STATE_HEADER1;  // 重置状态机
            log_warn("Frame timeout, reset to IDLE");
        }
    }
}
```

### Q04 �?Modbus TCP �?RTU 的区�?

**📐 L1　💡 高频：否　🔗 关联�?6#Q03**

**答案**�?

| 特�?| Modbus RTU | Modbus TCP |
||--|
| 物理�?| RS485/RS232 | 以太�?|
| 帧边�?| 字符间隔�?.5 字符�?| TCP �?|
| 地址 | 1-247（从站地址�?| 单元标识�?|
| CRC | CRC-16 校验 | 无（TCP 保证可靠�?|
| 模式 | 主从 | 客户�?服务�?|

## 二、CAN 总线

### Q06 �?CAN 基础（标识符、波特率、采样点�?

**📐 L1　💡 高频：是　🔗 关联�?4#Q01（CAN 引脚�?*

**场景**：你需要配�?CAN 总线通信�?

**答案**�?
```
CAN 总线�?
- 差分信号（CAN_H, CAN_L），抗干扰强
- 多主结构，标识符决定优先�?
- 波特率：125k/250k/500k/1Mbps
- 采样点：通常 75-87.5%
- 标准帧（11 �?ID）和扩展帧（29 �?ID�?
```

```c
// CAN 波特率配置（�?42MHz CAN 时钟�?00kbps 为例�?
// 目标�?00kbps，采样点 87.5%
// 方案：Prescaler=6, BS1=5, BS2=1
// 波特�?= 42MHz / (Prescaler) / (1 + BS1 + BS2) = 42MHz / 6 / 7 = 1Mbps... 不对
// 
// 正确方案：Prescaler=6, BS1=8, BS2=1
// 波特�?= 42MHz / 6 / (1 + 8 + 1) = 42MHz / 6 / 10 = 700kbps
//
// 最终正确方案：Prescaler=6, BS1=12, BS2=1
// 波特�?= 42MHz / 6 / (1 + 12 + 1) = 42MHz / 6 / 14 = 500kbps �?
// 采样�?= (1 + BS1) / (1 + BS1 + BS2) = 13/14 �?92.9%（偏高）
//
// 更精确方案：Prescaler=12, BS1=7, BS2=1
// 波特�?= 42MHz / 12 / (1 + 7 + 1) = 42MHz / 12 / 9 = 388.9kbps...
// 
// 最佳实践：使用 STM32CubeMX �?CAN 波特率计算器
// 手动计算时：
// 1. 选择时间份额 TQ = (1 + BS1 + BS2)，通常 8~25
// 2. Prescaler = CAN_CLK / (波特�?× TQ)
// 3. 采样�?= (1 + BS1) / TQ，目�?75%~90%
hcan.Init.Prescaler = 6;     // 42MHz / 6 = 7MHz 基础时钟
hcan.Init.TimeSeg1 = 11;     // BS1 = 11
hcan.Init.TimeSeg2 = 2;      // BS2 = 2
// 波特�?= 7MHz / (1 + 11 + 2) = 7MHz / 14 = 500kbps
// 采样�?= (1 + 11) / 14 = 85.7%
```

### Q08 �?CAN 错误处理与总线关闭恢复

**📐 L2　💡 高频：否　🔗 关联�?8#Q06**

**场景**：你�?CAN 节点偶尔进入总线关闭状态�?

**答案**�?
```
CAN 错误状态：
- 错误警告（TEC > 96）：总线有干�?
- 被动错误（TEC > 127）：节点只能发送消极错误帧
- 总线关闭（TEC > 255）：节点自动脱离总线
恢复：硬件自动恢复（128 �?11 位隐性位），或软件请求退出总线关闭
```

```c
// 检�?CAN 错误状�?
void can_error_check(void)
{
    uint32_t error = HAL_CAN_GetError(&hcan);
    if (error & HAL_CAN_ERROR_BOF) {
        // 总线关闭：尝试恢�?
        HAL_CAN_Stop(&hcan);
        HAL_CAN_Start(&hcan);
    }
}
```

## 三、SPI/I2C 进阶

### Q10 �?SPI 多从机切换的时序问题

**📐 L2　💡 高频：是　🔗 关联�?4#Q14**

**场景**：你�?SPI 连接多个从机，切换通信时偶尔出现数据错位�?

**答案**�?
```
切换从机时需注意�?
1. 当前传输必须完成（BSY 标志清除�?
2. CS 引脚的拉�?拉高要在 SCK 空闲时操�?
3. 切换 CS 后延时一小段时间（从机设置时�?t_SU�?
4. CPOL/CPHA 模式可能不同，需重新配置 SPI
```

```c
void spi_select_slave(uint8_t slave)
{
    // 等待当前传输完成
    while (__HAL_SPI_GET_FLAG(&hspi1, SPI_FLAG_BSY));
    
    // 释放之前�?CS
    HAL_GPIO_WritePin(SPI_CS1_GPIO_Port, SPI_CS1_Pin, GPIO_PIN_SET);
    
    // 切换 SPI 模式（如果需要）
    if (slave == SENSOR_IMU) {
        hspi1.Init.CLKPolarity = SPI_POLARITY_HIGH;
        hspi1.Init.CLKPhase = SPI_PHASE_2EDGE;
    } else if (slave == SENSOR_MAG) {
        hspi1.Init.CLKPolarity = SPI_POLARITY_LOW;
        hspi1.Init.CLKPhase = SPI_PHASE_1EDGE;
    }
    HAL_SPI_Init(&hspi1);
    
    // 延时
    delay_us(1);
    
    // 拉低新的 CS
    if (slave == SENSOR_IMU) 
        HAL_GPIO_WritePin(SPI_CS1_GPIO_Port, SPI_CS1_Pin, GPIO_PIN_RESET);
    else 
        HAL_GPIO_WritePin(SPI_CS2_GPIO_Port, SPI_CS2_Pin, GPIO_PIN_RESET);
}
```

### Q12 �?SPI/I2C �?DMA 双缓冲策�?

**📐 L2　💡 高频：否　🔗 关联�?3#Q08�?3#Q09**

**场景**：你需要持续接�?SPI 数据，不能丢失任何数据�?

**答案**�?
```c
// 使用 DMA 双缓冲模�?
uint8_t spi_rx_buf_a[256];
uint8_t spi_rx_buf_b[256];

// 配置双缓�?DMA
HAL_DMAEx_MultiBufferStart(&hdma_spi1_rx, 
    (uint32_t)&SPI1->DR, 
    (uint32_t)spi_rx_buf_a, 
    (uint32_t)spi_rx_buf_b, 
    256);

// 半传输中断：缓冲�?A �?
// 传输完成中断：缓冲区 B �?
// CPU 可以安全处理一个缓冲区，DMA 同时写入另一�?
```

--|### Q14 �?MQTT 基础

**📐 L1　💡 高频：否　🔗 关联�?6#Q13**

**场景**：你的设备需要上报数据到云平台�?

**答案**�?
```
MQTT（Message Queuing Telemetry Transport）：
- 发布/订阅模式
- 三个 QoS 等级�?
  QoS 0：最多一次（fire and forget�?
  QoS 1：至少一次（需要确认，可能重复�?
  QoS 2：恰好一次（最可靠，开销最大）
- 遗嘱消息（Last Will）：设备异常断开时通知
- Keep Alive：心跳保�?
- 主题（Topic）："sensor/temp/room1"
```

> **本文件总结**：通信协议的核心是"帧边界检�?+ 错误处理 + 状态机"。重点掌握：UART 状态机设计、Modbus RTU/CAN 基础、协议错误恢复机制、前向兼容性设计�?
