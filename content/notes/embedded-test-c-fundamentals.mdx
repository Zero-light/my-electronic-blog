---
title: 嵌入式面试测试题 �?C 语言核心
slug: embedded-test-c-fundamentals
date: 2026-07-13
tags: ["C语言", "指针", "内存管理", "位操作", "volatile", "数据结构", "面试题"]
mainCategory: 测试题
subCategory: 软件
description: 嵌入式 C 语言面试测试题：指针与数组、内存分区、位操作与寄存器编程、未定义行为、数据结构在嵌入式中的应用。
---
﻿> **定位**：嵌入式面试�?C 语言是地基，80% �?�?�?交叉题都从这里出发�?
> **互联**：本文件是后续所有文件的语言基础。`volatile` �?03-中断；内存布局 �?02-启动流程；位操作 �?04-外设寄存器�?
> **难度分布**：L1 ~60% / L2 ~30% / L3 ~10%

--|--|
| `const char *p` | 指向常量的指�?| �?| �?|
| `char *const p` | 常量指针 | �?| �?|
| `const char *const p` | 指向常量的常量指�?| �?| �?|

**记忆口诀**：`const` 看它�?`*` 的哪一�?�?在左边锁内容，在右边锁指针�?

**工程应用**�?
```c
// 查找表放 Flash，编译器会将其放�?.rodata
const uint8_t crc_table[256] = { /* ... */ };

// 函数参数：承诺不修改传入的缓冲区
uint16_t calculate_crc(const uint8_t *buf, uint16_t len);
```

### Q03 �?`sizeof` 与指针的那些�?

**📐 L1　💡 高频：是　🔗 关联�?1#Q02�?2#Q14（栈大小计算�?*

**场景**：你�?64 �?PC 上测�?`malloc(10 * sizeof(int*))` 得到 80 字节，移植到 32 �?MCU 上变�?40 字节�?

**答案**�?
- `sizeof(void*)` �?32 �?MCU 上是 4 字节，在 64 �?PC 上是 8 字节
- `sizeof(int)` 在大多数嵌入式平台是 4 字节，但某些 DSP 上是 2 字节
- 嵌入式开发中应使�?`<stdint.h>` 中的 `uint32_t`、`int16_t` 等明确类�?

**高频面试追问**�?
```c
int *p = malloc(10 * sizeof(int));
// 如何安全地获取元素个数？
#define ARRAY_SIZE(x) (sizeof(x) / sizeof((x)[0]))  // 仅对真实数组有效
```

### Q05 �?`void*` 与通用容器

**📐 L1　💡 高频：是　🔗 关联�?5#Q18（消息队列传递任意类型）�?6#Q05（协议解析上下文�?*

**场景**：FreeRTOS �?`xQueueSend()` 需要传递任意类型的数据，它的参数是 `const void *pvItemToQueue`�?

**答案**�?
- `void*` 是通用指针，可以指向任何数据类�?
- 不能直接解引用，必须先强制转�?
- 不能进行指针算术（因�?`void` 大小未知�?

```c
// 通用缓冲区传�?
typedef struct {
    uint8_t  type;
    uint16_t len;
    uint8_t  data[];  // 柔性数�?
} Message_t;

void send_message(const void *msg, size_t len)
{
    HAL_UART_Transmit_DMA(&huart1, (uint8_t*)msg, len);
}
```

-||
| `.text` | 代码 + 常量 | Flash 加载 | 永久 |
| `.data` | 已初始化的全局/静态变�?| Flash �?RAM 拷贝 | 永久 |
| `.bss` | 未初始化的全局/静态变�?| 启动时清�?| 永久 |
| `.stack` | 局部变量、函数参数、返回地址 | 动�?| 函数作用�?|
| `.heap` | `malloc` 分配的内�?| 动�?| 手动管理 |

**启动文件中的关键操作**�?
```text
; startup_stm32xxxx.s �?Reset_Handler 会做�?
; 1. �?Flash 拷贝 .data �?RAM
; 2. �?.bss 段清�?
; 3. 调用 SystemInit()
; 4. 调用 main()
```

### Q08 �?`static` 的三种用�?

**📐 L1　💡 高频：是　🔗 关联�?1#Q09（信息隐藏）�?5#Q03（任务局部存储）**

**场景**：你在写一个模块化的驱动库，希望某些函数和变量只在本文件内可见�?

**答案**�?

| 用法 | 效果 |
|||
| 函数内的 `static` 变量 | 持久存储，但作用域限于函数内（只初始化一次） |
| 文件内的 `static` 函数 | 仅本文件可见（内部链接），实现信息隐�?|
| 文件内的 `static` 全局变量 | 仅本文件可见，其他文件无�?`extern` 引用 |

```c
// uart_driver.c
static uint8_t tx_buffer[256];  // 外部不可�?
static void uart_dma_config(void) {}  // 外部不可调用

uint32_t uart_get_error_count(void)
{
    static uint32_t error_count = 0;  // 持久，但只在此函数内可见
    return error_count++;
}
```

### Q10 �?栈与堆的区别及嵌入式中的使用策略

**📐 L1　💡 高频：是　🔗 关联�?1#Q06�?2#Q14�?8#Q03**

**场景**：你�?RTOS 任务�?`malloc()` 突然返回 `NULL`，但系统还有很多 RAM�?

**答案**�?

| 特�?| �?(Stack) | �?(Heap) |
||-|
| 分配/释放 | 自动（函数进�?退出） | 手动（`malloc`/`free`�?|
| 速度 | 极快（SP 移动�?| 较慢（查找空闲块�?|
| 大小 | 通常较小（几百字节~几KB�?| 较大（取决于空闲 RAM�?|
| 碎片 | �?| 有（长时间运行后严重�?|
| 溢出风险 | 有（递归/大局部变量） | 有（内存泄漏�?|

**嵌入式最佳实�?*�?
1. **避免�?RTOS 任务中使�?`malloc`** �?使用静态分配或固定块内存池
2. 如果必须用，使用 `pvPortMalloc()`（FreeRTOS 的线程安全版本）
3. 栈大小在创建任务时指定，需考虑最大调用深�?+ 局部变�?+ 中断嵌套

### Q12 �?大小端（Endianness�?

**📐 L1　💡 高频：是　🔗 关联�?4#Q15（ADC 数据）�?6#Q03（协议字节序�?*

**场景**：STM32 是小端模式，你通过 I2C 读取一个传感器的大�?16 位数据，直接赋值后数值不对�?

**答案**�?
- **小端（Little-Endian�?*：低字节存低地址（ARM Cortex-M 默认�?
- **大端（Big-Endian�?*：高字节存低地址（网络协议、部分传感器�?

```c
// 传感器返�?[0x12, 0x34] 表示 0x1234（大端）
uint8_t raw[2];
HAL_I2C_Mem_Read(&hi2c1, addr, reg, 1, raw, 2, 100);

// 正确做法：明确字节序转换
uint16_t val = ((uint16_t)raw[0] << 8) | raw[1];  // 大端转主机序
// 或使用宏
#define BIG_ENDIAN_TO_U16(p) (((uint16_t)(p)[0] << 8) | (p)[1])
```

### Q14 �?使用位域（Bit-field）操作寄存器

**📐 L2　💡 高频：是　🔗 关联�?4#Q01�?4#Q12（外设寄存器结构体）**

**场景**：你看到 STM32 头文件中 `GPIO_TypeDef` 的定义�?

**答案**�?
```c
// CMSIS 头文件中的寄存器定义（简化）
typedef struct {
    __IO uint32_t MODER;    // 0x00  模式寄存�?
    __IO uint32_t OTYPER;   // 0x04  输出类型
    __IO uint32_t OSPEEDR;  // 0x08  输出速度
    __IO uint32_t PUPDR;    // 0x0C  上拉/下拉
    __IO uint32_t IDR;      // 0x10  输入数据
    __IO uint32_t ODR;      // 0x14  输出数据
    __IO uint32_t BSRR;     // 0x18  位设�?复位（原子操作！�?
    __IO uint32_t LCKR;     // 0x1C  配置锁定
    __IO uint32_t AFR[2];   // 0x20  复用功能
} GPIO_TypeDef;

// 使用 BSRR 实现原子置位/清零（无需�?�?写）
GPIOA->BSRR = (1UL << 5);        // 置位 Pin5
GPIOA->BSRR = (1UL << (5 + 16)); // 清零 Pin5（高 16 位是复位�?
```

**面试追问**：`__IO` 是什么？
> 就是 `volatile` 的宏定义。`#define __IO volatile`

### Q16 �?`static inline` vs �?

**📐 L2　💡 高频：是　🔗 关联�?1#Q15**

**答案**�?

| 特�?| �?| `static inline` |
||--|
| 类型检�?| �?�?| �?�?|
| 副作用风�?| �?有（参数多次求值） | �?�?|
| 调试 | �?难（预处理器展开�?| �?可设断点 |
| 代码体积 | 每处展开 | 编译器决定是否内�?|
| 编译时计�?| �?可用于常量表达式 | �?不行 |

```c
// 推荐：嵌入式中优先使�?static inline
static inline void gpio_write(GPIO_TypeDef *port, uint8_t pin, uint8_t val)
{
    if (val)
        port->BSRR = (1UL << pin);
    else
        port->BSRR = (1UL << (pin + 16));
}
```

-|||
| 有符号整数溢�?| 编译器可能优化掉溢出检�?| 使用无符号类型或显式检�?|
| 空指针解引用 | HardFault | 检查指针有效�?|
| 数组越界 | �?堆损�?| 边界检�?|
| 使用未初始化变量 | 随机�?| 始终初始�?|
| 数据竞争（无保护的多线程访问�?| 数据损坏 | 使用临界�?原子操作 |
| 违反严格别名规则 | 优化后数据错�?| 使用 `__attribute__((aligned))` �?`memcpy` |

### Q19 �?有符号与无符号混用的陷阱

**📐 L1　💡 高频：是　🔗 关联�?4#Q07（ADC 采样值比较）�?5#Q15（tick 比较�?*

**场景**：你�?`if (tick_diff > timeout)` 判断超时，但 `tick_diff` �?`int32_t`，`timeout` �?`uint32_t`，结果永远不成立�?

**答案**�?
当有符号和无符号混用时，C 会将有符号隐式转换为无符号。负数变成很大的正数�?

```c
int32_t  a = -1;
uint32_t b = 100;

if (a < b) {
    // 不会执行！因�?a 被转�?uint32_t 变成 0xFFFFFFFF
}

// �?显式转换或统一类型
if ((uint32_t)a < b) { }  // 明确意图
```

**嵌入式经典场�?*�?
```c
// 32 �?tick 计数器回绕处�?
uint32_t start = HAL_GetTick();
// ... 执行操作 ...
uint32_t elapsed = HAL_GetTick() - start;  // 无符号减法天然处理回�?
if (elapsed >= timeout_ms) { /* 超时 */ }
```

||## 五、数据结构在嵌入式中的应�?

### Q21 �?环形缓冲区（Ring Buffer / Circular Buffer�?

**📐 L1　💡 高频：是　🔗 关联�?3#Q06（UART 接收）�?6#Q01（数据帧缓冲�?*

**场景**：UART 中断中接收字节，主循环中处理，需要一个缓冲区�?

**答案**�?
```c
typedef struct {
    uint8_t  *buffer;
    uint16_t  head;       // 写入位置
    uint16_t  tail;       // 读取位置
    uint16_t  size;       // 缓冲区大�?
} RingBuffer_t;

bool ring_buf_write(RingBuffer_t *rb, uint8_t data)
{
    uint16_t next = (rb->head + 1) % rb->size;
    if (next == rb->tail)  // �?
        return false;
    rb->buffer[rb->head] = data;
    rb->head = next;
    return true;
}

bool ring_buf_read(RingBuffer_t *rb, uint8_t *data)
{
    if (rb->head == rb->tail)  // �?
        return false;
    *data = rb->buffer[rb->tail];
    rb->tail = (rb->tail + 1) % rb->size;
    return true;
}
```

**面试追问**：如何判断满/空？
> 两种方案�?1) 浪费一个单元：`(head+1)%size == tail` 为满�?2) 使用额外计数器。方�?1 更常见�?

### Q23 �?状态机实现（switch-case vs 函数指针表）

**📐 L2　💡 高频：是　🔗 关联�?6#Q04（协议状态机）�?7#Q08（Bootloader 状态机�?*

**场景**：你需要实现一�?UART 帧解析器，根据当前状态处理接收到的字节�?

**答案**�?

**方案 1：switch-case（最常见�?*
```c
typedef enum {
    STATE_IDLE,
    STATE_HEADER,
    STATE_LENGTH,
    STATE_DATA,
    STATE_CRC
} ParserState_t;

void parser_feed(ParserState_t *state, uint8_t byte)
{
    switch (*state) {
        case STATE_IDLE:
            if (byte == 0xAA) *state = STATE_HEADER;
            break;
        case STATE_HEADER:
            /* ... */
            break;
        // ...
    }
}
```

**方案 2：函数指针表（高效，适合复杂状态机�?*
```c
typedef void (*StateFunc_t)(uint8_t byte);

void state_idle(uint8_t byte);
void state_header(uint8_t byte);
void state_length(uint8_t byte);

static const StateFunc_t state_table[] = {
    [STATE_IDLE]   = state_idle,
    [STATE_HEADER] = state_header,
    [STATE_LENGTH] = state_length,
};

// 调用
state_table[current_state](received_byte);
```

--|### Q25 �?`union` 的用�?

**📐 L2　💡 高频：是　🔗 关联�?4#Q15（ADC 多通道数据）�?6#Q03（协议字节解析）**

**场景**：你需要将一�?32 位寄存器值按字节或按位域访问�?

**答案**�?
```c
// 方式 1：寄存器多视图访�?
typedef union {
    uint32_t word;
    uint8_t  bytes[4];
    struct {
        uint32_t enable : 1;
        uint32_t mode   : 3;
        uint32_t presc  : 8;
        uint32_t rsvd   : 20;
    } bits;
} ControlReg_t;

// 方式 2：协议字节序转换
union {
    uint32_t u32;
    uint8_t  u8[4];
} endian;
endian.u32 = 0x12345678;
// 在小�?MCU 上：u8[0]=0x78, u8[1]=0x56, u8[2]=0x34, u8[3]=0x12
```

### Q27 �?柔性数组（Flexible Array Member�?

**📐 L2　💡 高频：否　🔗 关联�?1#Q05�?5#Q18（消息队列）**

**场景**：你需要一个变长数据包结构体，长度在运行时确定�?

**答案**�?
```c
typedef struct {
    uint8_t  type;
    uint16_t len;
    uint8_t  data[];  // 柔性数组：必须是最后一个成员，不占 sizeof 空间
} Packet_t;

// 动态分�?
Packet_t *pkt = malloc(sizeof(Packet_t) + payload_len);
pkt->len = payload_len;
memcpy(pkt->data, payload, payload_len);
```

### Q29 �?实现一个简化的断言�?

**📐 L1　💡 高频：是　🔗 关联�?8#Q01（HardFault 调试�?*

**场景**：你需要在参数错误时快速定位问题，但不想用标准 `assert`（可能触发不可控行为）�?

**答案**�?
```c
#ifdef DEBUG
    #define ASSERT(expr) \
        do { \
            if (!(expr)) { \
                __disable_irq(); \
                uart_printf(&huart1, "ASSERT FAIL: %s:%d %s\n", \
                    __FILE__, __LINE__, #expr); \
                while(1); \
            } \
        } while(0)
#else
    #define ASSERT(expr) ((void)0)
#endif
```

> **本文件总结**：C 语言是嵌入式�?空气" �?无处不在但容易被忽视。面试中 80% �?为什么这样写"问题，答案都在本文件的某个知识点里。重点掌握：`volatile`、指针与数组、内存布局、位操作、`static` 语义�?
